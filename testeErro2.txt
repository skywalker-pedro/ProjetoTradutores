float teste(){
    a = a +1;

return 1;
}}

int main(){
    int x;
    int y;
    int a;
    x = 1;
    y = x;
    a = x * b;
    set conj;
    conj = EMPTY;
    add(x in conj);
    is_set(conj);
    remove(x in conj);
    x=2;
    y=4;
    if(x==2)
        writeln(1);
    if(x==3)
        writeln(2);
    x = x + 2;
    writeln(((x+x+1+1)/2)*y);
return 0;
}