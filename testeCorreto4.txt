int main(){
    int x;
    float y;
    x=2.3;
    y=4;
    if(x==2)
        writeln(1);
    if(x!=2)
        writeln(2);
    else
        writeln(200);
    x = x + 2;
    writeln(((x+x+1+1)/2)*y);
return 0;
}