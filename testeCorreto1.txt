int main(){
    int x;
    int y;
    set g;
    is_set(g);
    add(x in g);
    x=3.5;
    y=4.5;
    if(x==2.5) writeln(1);
    if(x==3.3) writeln(2);
    x = x + 2;
    writeln(((x+x+1+1)/2)*y);
return 0;
}