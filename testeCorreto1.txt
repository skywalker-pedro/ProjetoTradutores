int main(){
    int x;
    int y;
    set g;
    is_set(g);
    add(x in g);
    x=2;
    y=4;
    if(x==2) writeln(1);
    if(x==3) writeln(2);
    x = x + 2;
    writeln(((x+x+1+1)/2)*y);
return 0;
}